package model;

public class Athlete extends User {
    public Athlete(String login, String password, String firstName, String lastName) {
        super(login, password, firstName, lastName);
    }
}
